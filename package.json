{
  "name": "linkedin-employee",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "db:types": "echo 'Types are hand-maintained: our tables live in the le schema, and gen types emits public. See packages/db/src/database.types.ts' && exit 1"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.9.0"
  }
}
